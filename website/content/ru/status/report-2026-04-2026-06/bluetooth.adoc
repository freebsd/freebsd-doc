=== Драйверы HID для Bluetooth

Ссылки:

link:https://wiki.freebsd.org/SummerOfCode2026Projects/BluetoothSupportForHIDDevices[Страница проекта в вики FreeBSD] URL: link:https://wiki.freebsd.org/SummerOfCode2026Projects/BluetoothSupportForHIDDevices[] +
link:https://github.com/majintosh/freebsd-bthid[Репозиторий проекта на Github] URL: link:https://github.com/majintosh/freebsd-bthid[]

Контактное лицо: Majed Alkhaleefah <majed@FreeBSD.org>

Я работаю над двумя драйверами для обеспечения поддержки HID-драйверов для устройств Bluetooth в рамках GSoC 2026.

Основная цель этого проекта — регистрация подключаемых Bluetooth-устройств HID как устройств Newbus с помощью двух новых драйверов: `bthidbus` и `bthid`.
`bthidbus` выступает в роли родительской шины, управляющей созданием и подключением дочерних устройств `bthid`.
`bthid` служит транспортным уровнем, создавая устройство `hidbus` и перенаправляя на него все полученные пакеты после удаления их заголовков.
Краткосрочная цель здесь — обеспечить поддержку беспроводных HID-подключений.
Долгосрочная цель — начать закладывать основу, которая позволит другим писать драйверы для устройств, подключаемых только через Bluetooth.

Что было сделано:

* `bthidbus` может успешно открывать соединение с Bluetooth-устройством через сокеты.
* `bthid` может принимать сокеты, переданные от `bthidbus` через ivars, и управлять ими.
* `bthid` может отправлять дескриптор отчёта в `hidbus`, который успешно создаёт соответствующее HID-устройство и драйвер.
(например, геймпад, подключённый по Bluetooth, создаёт устройство `hgame` и узел `evdev`.)
* Ввод с Bluetooth-устройств успешно принимается `hidbus` и `evdev`.

Текущие проблемы и планируемые изменения:

* Фактические дескрипторы отчётов, Bluetooth-адреса и т.д. должны быть жёстко закодированы в `bthidbus`.
Изначально я рассматривал возможность добавления логики запросов/прослушивания в `bthidbus`, чтобы он действовал как псевдо-сервер, но я опасался, что это приведёт к раздуванию кода и расширению области ответственности.
Вместо этого я планирую положиться на существующую логику запросов демона `bthidd` и передавать новую информацию о подключении в `bthidbus` через `ioctl`.
Это избавит `bthidbus` от текущей роли инициатора соединения и сделает его ответственным только за управление уже открытыми соединениями.
* Наблюдается значительная задержка ввода при мониторинге через `evtest`.
У меня есть две рабочие гипотезы.
Первая заключается в том, что проблема кроется в `bthid` и в том, как он управляет прерываниями через очереди задач (taskqueues).
Вторая — что проблема в сетевом стеке, поскольку Bluetooth-пакеты передаются через несколько различных узлов Netgraph перед тем, как быть полученными `bthid`.
Также вполне возможно, что это проблема `evtest`; мне ещё предстоит это выяснить.

На данный момент все тесты проводились с контроллером Nintendo Switch Pro.

//
// The FreeBSD Russian Documentation Project
//
// Original EN revision (13.07.2026): d3ebf769a3b1d7f5f6c6f17118ffa358fb65ff9a
//
